import { Shirt } from 'lucide-react'

export default () => {
	return (
		<div className="fixed inset-0 z-50 flex items-center justify-center">
			<Shirt className="size-36 animate-spin text-primary opacity-60" />
		</div>
	)
}
